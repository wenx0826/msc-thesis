v1
So, hello and welcome to my, I'm Xin, and welcome to my final thesis presentation on topic Document-Based Process Modeler. Let's get in with the motivation. As with the rise of large language models, many AI-assisted tools have emerged, which have greatly improved efficiency of process modeling. However, most of these tools are primarily designed for text input. A few tools have started to support document upload, but this is not usually their main design focus. However, at the same time, the real-world data in real world, the process information is usually stored in documents. And besides, the current tools only support one single modeling, one single model, which may make it less flexible to decompose large processes into sub-processes or connect it with related part, especially for solving the complex real-world scenarios. So with this limitation, the current AI-assisted tools are still not ideal for process modeling. But before we go any further to this topic, let me briefly introduce the current existing AI-assisted tools, first in academia. So this table shows most of the existing tools. The very beginning, we can know is the tool called Promo AI and published in 2024. And at that time, it can already support business automated generation. It can also support iterative refinement. But as it is in the academic area, most tools are focusing. They are accompanying with as a side tool for their key approach, focusing on... Scientific aspects such as model correctness, quality, token efficiency, or prompt strategies. Here, I would like to mention a few examples with distinguished features only. The first is AutoBPM AI, and it is the only one among these tools that can support file upload, but it only supports one document in TXT file at the moment. And after uploading, you can edit the text, but if you do so, you may lose the original source context of the original document when reviewing. The second one, this tool has can support extend the input tab to audio upload. However, it would, but it will take the audio as one big process and unable to extract the format part of it. This tool extends the feature, feature wise, it introduces the arrow detection, arrow detection module, and it also the first one could support the collaboration between different links. The last one I would like to introduce is this tool, it can support party query, as you can select the part and then do some advanced query of ask for extended build. Excellent. And as for explanation, I think. In the industry, the one commercial tool I found recently that already supports document upload, as you can see with appropriate prompts, it allows you to extract the required information from documents directly. But although the documents upload is supported, it is still not ideal for process modeling from documents. The main challenges include difficulty in verifying document correctness and lack of the source and lack of the traceability between the source text and model. Besides that, in the real-world scenario, one process could be described across multiple documents or one document may contain several subprocesses. So the current tree is insufficient and inefficient. To address these challenges, we would like to propose this solution. And firstly, it should support upload multiple documents. Then you should be able to view the content and only select the needed parts from the uploaded documents and then use it as LLM input directly. Here we will leverage the... AutoBPM is a right service directly to generate the model. And then after that, you will see a visual link between the exactly trace between the source text and the model whenever you want to review them. Finally, we would introduce this multiple document, multiple model management in one unified workspace to support a more seamless working flow for the scenario document-based process modeling. And then besides the basic requirements we elicited, for further some features we designed is, the first one is the iterative refinement as it is a basic requirement for AI. I think it is one of the basis requirements for any AI-assisted tools with this dialogue. And then as some special one we want to enable is that documents naturally involved versioning and have versioning thing. Therefore, we would like to support document upload. Then if the document has versioning, we would like to Then the model should also correspond to it to support versioning as well. Lastly, we introduced here, we introduced a graph visualization tool. a graph to visualize the relationship within this workspace, and one special part is it will be especially helpful to indicate the process and sub-process relationship. Before we move into the prototype demo, 

------
Let me begin with the motivation. So with the rise of AI-assisted, with the rise of large language models, many AI-assisted tools have emerged recently, which have greatly improved the efficiency of process modeling. However, most of these tools currently only support text input, although a few have started to support file upload, but this is usually not their primary design goal. At the same time, in practice, at the same time, the real-world data usually stored in the real-world data, in real-world, the process information is usually stored in documents. And besides, the current tools usually focus on single models. This makes it less flexible to decompose one big, one large process into sub-processes or to connect it with related models, especially for solving complex, robust problems. So with these limitations, the current AI-assist tools is still not so ideal for process modeling from documents. Before going further, let me... First, briefly introduce you to some of the current landscape of AI-assisted modeling tools. First, in academia, this table shows most of existing tools. The first, the very early one is promo AI I can find, and it is published in 2024. And at that time, besides automated model generation, it can also support iterative refinement at that time. But since then, since they are scientific tools, what their key contribution is or key focus is on aspects like model correctness, quality, token efficiency, or prompt strategies. So next, I will only introduce some distinguished features related, may interesting, of these tools. The first is AI-assist, the first is AutoBPMN AI, and it is actually the only one among these tools that could support file upload. But it only supports one document in TXT format. And after uploading, you can edit the text if you want, but you will lose the exact source and context of the original document when reviewing. This one is, this tool makes one extension on the input type. It supports audio upload, but it will treat the whole upload as One process only. As you see, there is no additional prompt input for it. And this tool has some distinct feature as it introduced an error detection module. And besides that, it is to contribute on the collaboration between the BPMN links. And the last one I would like to introduce is this tool already enabled the query function, which you can select parts of it and ask for some explanation. Then in the industry side, this is true. I found recently it can already support document upload with some appropriate prompt. it is able to extract the part to extract the part of the information, extract the required text from the document. However, although these tools can enable the document upload, it is for the scenario, document-based process modeling is still have some challenges. Firstly, as this way is quite, it is solving this problem is in quite imaginable way, as it is The remaining issue for it is you could not, you will have to, like it still have challenge, like it is hard to review as to review the correctness of the model. And you, and you also if lack, it, it lacks the traceability between the source text and the model. Besides of that, the one model view issue we have already introduced, like in the real world, one process could consist of, could be described across multiple documents, and one document could be contained several sub-processes that should be modeled separately. So, which is making it, which making the, if you want to model such scenario, the current tools will rest this inefficient. So for this scenario, the current, the current existing tools is solving it in very inefficient way. To address these problems, we would like to propose a different way to solve this problem. Firstly, it should support multiple documents upload firstly, and then it will enable you to view the content of it. And after that, you could select only the needed part from the text segment from the, from the document and then use it directly as input of the LM service. For other solution, we will leverage the REST API service from AutoBit and AI directly. And then after a model is... is generated, it will link with the source tags with a persistent DB relationship, so you can always review it with a clear visualized link whenever you want to review it. Finally, as you can see, we would like to provide a unified workspace for this modeling workflow for all the related documents and models for one link, then it will offer the functionality to allow you to link it between the different models. Besides that, some features we would like to offer is, firstly, is this iterative refinement, which is the basic requirements for all the, it's a basic functionality for all the AI-assisted modeling tools. Besides that, one special feature for this tool is, as we are a document-oriented design and documenting was versioning naturally, so we will support the feature like document update. And then if documents can support versioning, then that's naturally the model should enable this, should enable model control as well. Lastly, I would like to, lastly, we design a graph, we design a graph for it. We want to provide a graph visualization as well to visualize the relationship of the current working case. It will be especially helpful for indicate the relationship between the models. Now let's move to the product demo directly. But before we jump to the demo, let me give you an introduction to the system firstly. The final system
---

