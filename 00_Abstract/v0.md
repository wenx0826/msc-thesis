
We propose a unified and integrated environment that combines document interaction and process modeling within a coherent workflow, enabling users to create and manage process models directly from textual documents.

---

This work proposes an integrated system that unifies document interaction and process modeling, allowing users to transform textual content into structured process models and to manage complex process relationships within a single workflow.

This work proposes an integrated system that combines document interaction and process modeling, enabling users to derive process models directly from textual content.

This work proposes an integrated system that combines document interaction and process modeling, supporting the transformation of textual content into structured process models within a unified workflow.


==This work proposes an integrated system that unifies document interaction and process modeling, enabling end-to-end transformation from textual content to structured process models within a single workflow.==

This work aims to support document-based process modeling by integrating document interaction and process model construction within a unified system.